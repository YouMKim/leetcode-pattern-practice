// Supabase project config. The ANON (publishable) key is safe to ship in a
// public frontend — all data access is enforced by Row-Level Security.
//
// Get the key: Supabase Dashboard → Project Settings → API Keys →
// "anon public" (legacy) or the sb_publishable_... key. Paste it below.
// While it is empty, the game runs in local-only mode (localStorage).

export const SUPABASE_URL = 'https://xkgnqnviaoysmicqywkf.supabase.co';
export const SUPABASE_ANON_KEY = '';
